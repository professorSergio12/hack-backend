# Target to keep awake (override if your Render URL changes)
KEEPALIVE_URL=https://hack-backend-h3eq.onrender.com/health

# Cron schedule (default: every 10 minutes)
KEEPALIVE_CRON=*/10 * * * *
CRON_TZ=UTC
KEEPALIVE_TIMEOUT_MS=45000
